GHOSTSHIP — PROGRESS CHECKLIST
================================
Instructions for AI agents:
- Mark tasks [x] when complete, add timestamp: [x] 10:45 AM — description
- Update NOTES/BLOCKERS section if you hit issues
- Do NOT modify files outside your assigned track
- Run verification command after completing each track
- If verification fails, fix before moving on

=====================================
PHASE 0: SETUP (DONE)
=====================================
[x] Next.js app scaffolded (App Router, TypeScript)
[x] Chat SDK installed with all adapters (v4.20.2)
[x] Slack adapter configured (bot token + signing secret)
[x] Slack app created with manifest (app_mention events, interactivity)
[x] Gemini API key obtained
[x] ngrok tunnel active for local webhook delivery
[x] Bot logic running (src/lib/bot.tsx — currently example app)
[x] .env.local in .gitignore

## Pre-Batch 1 Setup
[x] 10:40 AM — Installed deps: @ai-sdk/google, zod, puppeteer-core, @sparticuz/chromium
[x] 10:41 AM — Gemini API key verified: gemini-2.5-flash responds correctly
[x] 10:41 AM — Puppeteer verified: local Chrome screenshot of vercel.com → 488KB PNG
[x] 10:42 AM — Added GOOGLE_GENERATIVE_AI_API_KEY to .env.local (Vercel AI SDK expects this env var name)
[x] 10:42 AM — Build passes with new deps
[x] 10:42 AM — Created directories: src/components, src/app/api/evaluate

=====================================
BATCH 1: PARALLEL TRACKS (all independent)
=====================================
These 4 tracks have NO dependencies on each other.
They CAN and SHOULD run simultaneously.
Each track owns specific files — do not touch files outside your track.

## Track 1: Types + Personas [Agent A]
   Owner: src/lib/personas.ts (CREATE)
   [x] Define Persona type: { id, name, emoji, age, background, goals, evaluationCriteria, behaviorPattern }
   [x] Define PersonaResult type: { personaId, personaName, personaEmoji, preference, confidence, rationale, productionPros, productionCons, previewPros, previewCons }
   [x] Define GhostshipReport type: { winner, confidence, preferenceSplit, personas, summary, previewUrl, productionUrl }
   [x] Implement 5 personas with detailed system prompts:
       [x] Budget-Conscious Buyer
       [x] Power User / Developer
       [x] Non-Technical Executive
       [x] First-Time Visitor
       [x] Accessibility-Focused User
   VERIFY: pnpm typecheck passes with no errors in personas.ts ✓

## Track 2: Screenshot Service [Agent B]
   Owner: src/lib/screenshot.ts (CREATE)
   Deps: puppeteer-core, @sparticuz/chromium (no external API key needed)
   [x] Implement captureScreenshot(url: string): Promise<Buffer>
       — uses puppeteer-core with @sparticuz/chromium for serverless
       — local dev: uses locally installed Chrome/Chromium
       — production (Vercel): uses @sparticuz/chromium binary
       — viewport 1280x800, format=png, wait for network idle
       — returns PNG buffer
   [x] Implement parseVercelPreviewUrl(previewUrl: string): { previewUrl, productionUrl, pagePath }
       — pattern: {project}-git-{branch}.vercel.app → {project}.vercel.app
       — also handle: {project}-{hash}-{team}.vercel.app pattern
       — preserve the path (e.g., /pricing)
       — fallback: return null productionUrl if pattern doesn't match
   [x] Handle Chrome path detection:
       — macOS dev: /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
       — Vercel serverless: @sparticuz/chromium.executablePath()
       — detect environment via process.env.AWS_LAMBDA_FUNCTION_NAME or similar
   VERIFIED: screenshot of vercel.com → 487KB PNG at /tmp/ghostship-verify-screenshot.png
   VERIFIED: parseVercelPreviewUrl returns correct productionUrl + pagePath for all 3 patterns

## Track 3: Gemini Evaluation [Agent C]
   Owner: src/lib/evaluate.ts (CREATE)
   [x] 10:50 AM — Import types from personas.ts (Persona, PersonaResult)
   [x] 10:50 AM — Set up Vercel AI SDK with @ai-sdk/google provider (gemini-2.5-flash model)
   [x] 10:50 AM — Define zod schema matching PersonaResult type
   [x] 10:50 AM — Implement evaluateAsPersona(persona: Persona, productionPng: Buffer, previewPng: Buffer): Promise<PersonaResult>
       — builds system prompt from persona fields
       — sends both PNGs as image parts
       — uses generateObject() for structured output
       — returns typed PersonaResult
   VERIFY: pnpm typecheck passes — no errors in evaluate.ts ✓

## Track 7: Landing Page [Agent D]
   Owner: src/app/page.tsx (MODIFY — replace entirely)
   DO NOT create or touch: src/components/, src/lib/, src/app/api/
   [x] 11:15 AM — Replace example page.tsx with minimal landing page
       — Single section, centered vertically and horizontally, full viewport
       — Logo: public/images/logo-square.png (use next/image, ~120-160px)
       — Title: "ghostship" (lowercase, bold, large)
       — Subtitle: "Phantom users for every pull request."
       — One-liner: "Every Vercel preview is already an A/B test. It just has zero users."
   [x] 11:15 AM — Dark background (#0A0A0A) matching logo aesthetic
   [x] 11:15 AM — Clean minimal typography, logo and copy have breathing room
   [x] 11:15 AM — Responsive (centered on desktop and mobile)
   [x] 11:15 AM — Follow design skills: high-end-visual-design, design-taste-frontend
   [x] 11:15 AM — Added Tailwind CSS v4, globals.css, layout metadata (OG, favicons, Twitter cards)
   [x] 11:15 AM — Subtle radial glow behind ghost mascot using brand cyan
   VERIFIED: localhost:3000 shows GhostShip landing with logo + headline, visually confirmed via browser ✓

=====================================
BATCH 2: SEQUENTIAL TRACKS (after Batch 1)
=====================================
These depend on Batch 1 being complete.

## Track 4: Orchestrator [after Tracks 1, 2, 3]
   Owner: src/lib/agent.ts (CREATE)
   [x] Import: captureScreenshot, parseVercelPreviewUrl from screenshot.ts
   [x] Import: evaluateAsPersona from evaluate.ts
   [x] Import: personas, GhostshipReport from personas.ts
   [x] Implement runGhostship(previewUrl: string, productionUrl?: string): Promise<GhostshipReport>
       [x] Parse/derive URLs using parseVercelPreviewUrl
       [x] Capture both screenshots in parallel: Promise.all([capture(preview), capture(production)])
       [x] Run 5 persona evaluations in parallel: Promise.all(personas.map(...))
       [x] Aggregate results:
           — Count votes (production vs preview)
           — Determine winner (or "inconclusive" if 3-2 split with low confidence)
           — Compute confidence score (0-100)
           — Generate summary string
       [x] Return GhostshipReport
   VERIFIED: pnpm typecheck — no errors in agent.ts (pre-existing screenshot.ts type stubs issue only)

## Track 5: Bot Logic [after Track 4]
   Owner: src/lib/bot.tsx (MODIFY — replace example handlers)
   [x] 3:00 PM — Add URL regex pattern to detect URLs in mentions
   [x] 3:00 PM — Add onNewMention handler for GhostShip:
       — When @ghostship is mentioned with a URL:
         1. Post loading message: "Boarding your preview..."
         2. Call runGhostship(url)
         3. Format results as Chat SDK Card (JSX)
         4. Post report card to thread
       — When @ghostship is mentioned without a URL:
         1. Post help message explaining usage
   [x] 3:00 PM — Format report card using Chat SDK JSX components:
       — Card with title "Ghostship Report: /path"
       — Section per persona with emoji, preference, rationale
       — Divider
       — Summary section
   [x] 3:00 PM — Error handling: catch pipeline errors, post friendly error message
   [x] 3:00 PM — Keep example handlers (they respond to button actions, not mentions — no interference)
   VERIFIED: pnpm typecheck — no new errors in bot.tsx (pre-existing screenshot.ts type stubs only)

## Track 6: Web API Route [after Track 4]
   Owner: src/app/api/evaluate/route.ts (CREATE)
   [ ] POST handler: parse { previewUrl, productionUrl? } from request body
   [ ] Validate: previewUrl is required, is a valid URL
   [ ] Call runGhostship(previewUrl, productionUrl)
   [ ] Return JSON response with GhostshipReport
   [ ] Error handling: 400 for invalid input, 500 for pipeline errors
   VERIFY: curl -X POST localhost:3000/api/evaluate -H 'Content-Type: application/json' -d '{"previewUrl":"https://vercel.com"}' → valid JSON

=====================================
BATCH 2.5: PERSONA TESTING (independent, can run now)
=====================================

## Track 8: Single-Page Evaluation Script
   Owner: src/lib/evaluate.ts (MODIFY), src/lib/personas.ts (MODIFY), scripts/evaluate-page.ts (CREATE)
   Purpose: Validate persona quality by evaluating a single page (no A/B comparison)
   [x] Add PageEvaluation type to personas.ts (overallImpression, score, firstImpression, rationale, strengths, weaknesses, suggestions)
   [x] Add evaluatePage(persona, pagePng, pageUrl?) function to evaluate.ts
       — separate system prompt for single-page UX audit (not A/B comparison)
       — zod schema matching PageEvaluation
       — uses gemini-2.5-flash with structured output
   [x] Create scripts/evaluate-page.ts CLI script
       — takes URL as CLI arg
       — loads .env.local via dotenv
       — screenshots URL, runs all 5 personas in parallel
       — prints formatted results with scores, impressions, strengths/weaknesses
       — shows timing info
   VERIFIED: npx tsx scripts/evaluate-page.ts https://vercel.com
       — all 5 personas return differentiated feedback ✓
       — scores vary across personas (6, 9, 6, 8, 8) ✓
       — completes in 19.9s (< 60s) ✓

=====================================
BATCH 2.75: DOGFOODING (after deploy)
=====================================

## Track 9a: Pricing Page (Production Baseline)
   Owner: src/app/pricing/page.tsx (CREATE)
   [x] Create 3-tier pricing page at /pricing — done, deployed to production
   VERIFIED: https://www.ghostship.sh/pricing renders ✓

## Track 9b: Variant PRs (after 9a deployed to Vercel)
   [x] PR #1 — pricing-v3: Outcome-focused copy with testimonials (Vercel preview deployed ✓)
   [x] PR #2 — pricing-v2: Comparison table layout (Vercel preview deployed ✓)
   VERIFIED: 2 PRs open, both with successful Vercel preview deployments ✓

## Track 10: GitHub Adapter Integration
   Owner: src/lib/bot.tsx (MODIFY if needed), .env.local (add GitHub creds)
   [x] GitHub adapter already wired in adapters.ts (activates when GITHUB_WEBHOOK_SECRET is set)
   [x] Webhook route /api/webhooks/github already handles GitHub events via Chat SDK
   [x] Added Vercel preview URL auto-detection from PR comments (vercel[bot] or any .vercel.app URL)
   [x] onNewMention handler works cross-platform — explicit URL triggers pipeline on GitHub same as Slack
   [x] GitHub-specific flow: @ghostship without URL → scans PR comments for Vercel preview → runs pipeline
   [x] Report card renders as GitHub markdown via Chat SDK's renderFormatted()
   [x] pnpm typecheck passes
   [ ] Set up GitHub App or PAT (GITHUB_TOKEN + GITHUB_WEBHOOK_SECRET) in .env.local
   [ ] Configure GitHub webhook → /api/webhooks/github (issue_comment + pull_request events)
   [ ] Test: comment @ghostship <preview-url> on a PR → report card as PR comment
   VERIFY: report card appears as GitHub PR comment with persona breakdown

=====================================
BATCH 3: POLISH & SHIP
=====================================

## Error Handling Hardening
   [ ] Puppeteer/screenshot failure → graceful error in Slack
   [ ] Single persona Gemini failure → return partial results (4/5 ok)
   [ ] URL parsing failure → prompt user for both URLs explicitly
   [ ] Global timeout (60s) → return whatever results are ready

## Demo Prep
   [ ] Identify/create a Vercel project with a deployed PR preview
   [ ] PR should change something visually obvious (CTA, headline, layout)
   [ ] Run GhostShip against demo URLs 3+ times, confirm consistent quality
   [ ] Screen-record the Slack demo flow (backup)

## Deploy
   [ ] Deploy to Vercel: vercel deploy --prod
   [ ] Set env vars in Vercel dashboard
   [ ] Update Slack event subscription URL to production
   [ ] Test: @ghostship in Slack on production → works
   [ ] Test: production web URL → works

## (STRETCH) Web Compare UI [only if time permits]
   Owner: src/app/page.tsx, src/app/api/evaluate/route.ts, src/components/report-card.tsx
   [ ] Add URL input form below hero section on landing page
   [ ] Create /api/evaluate POST endpoint wired to runGhostship()
   [ ] Create report-card.tsx component (winner, personas, summary)
   [ ] Wire form → API → report card display
   VERIFY: paste URL in browser → click button → report card renders

## Submission
   [ ] README.md: problem, solution, tech stack, demo link
   [ ] Final review of PROBLEM_STATEMENT.md
   [ ] Submit before 5:00 PM

=====================================
BATCH 2.8: UNIFIED 3-CAPABILITY BOT
=====================================

## Track 13: Single-URL Page Review in Orchestrator + Bot
   Owner: src/lib/agent.ts (MODIFY), src/lib/bot.tsx (MODIFY), src/lib/personas.ts (MODIFY)
   Purpose: Wire evaluatePage() into orchestrator + bot so all 3 capabilities work from Slack/GitHub
   [ ] Add PageReviewReport type to personas.ts
   [ ] Add reviewPage(url) function to agent.ts
       — screenshots URL, runs all 5 personas via evaluatePage(), aggregates scores + summary
   [ ] Update bot.tsx mention handler routing:
       — 0 URLs + GitHub PR → runGhostshipForPR() (PR comparison)
       — 1 URL + Vercel preview → runGhostship() (auto-detect prod, A/B compare)
       — 1 URL + non-Vercel → reviewPage() (single page review)
       — 2 URLs → runGhostship(url1, url2) (explicit A/B compare)
   [ ] Add formatPageReview() Card layout for single-page results
       — persona scores, impressions, strengths/weaknesses, average score
   VERIFY: @ghostship https://stripe.com/pricing → single page review card
   VERIFY: @ghostship https://preview.vercel.app/pricing → A/B comparison card
   VERIFY: @ghostship url1 url2 → explicit comparison card

=====================================
ADVANCED: INTERACTIVE PERSONA AGENTS
=====================================

## Track 11: Agent-Browser Personas [stretch — only if time permits]
   Owner: src/lib/evaluate-interactive.ts (CREATE), scripts/evaluate-interactive.ts (CREATE)
   Deps: agent-browser (https://github.com/vercel-labs/agent-browser), Vercel AI SDK tool calling
   Purpose: Upgrade personas from screenshot observers → browsing agents that navigate, click, scroll
   [ ] Research agent-browser API and integration patterns
   [ ] Add InteractiveEvaluation + BrowserAction types to personas.ts
   [ ] Create evaluate-interactive.ts with evaluateInteractively(persona, url)
       — starts agent-browser session per persona
       — Gemini as tool-calling agent with browser tools (navigate, click, scroll, screenshot, readPage, type)
       — persona system prompt shapes browsing behavior
       — captures journey (actions + screenshots + reasoning)
       — returns InteractiveEvaluation with full action log
   [ ] Create scripts/evaluate-interactive.ts CLI test script
       — npx tsx scripts/evaluate-interactive.ts https://vercel.com/pricing
       — outputs each persona's journey with action-by-action narration
   [ ] Add mode flag to runGhostship(): "screenshot" (fast, 30s) vs "interactive" (deep, 2-5min)
   [ ] Add --deep flag to Slack/GitHub handler: @ghostship <url> --deep
   VERIFY: persona navigates page, takes 5-15 actions, produces behavioral evaluation
   VERIFY: each persona has different journey based on their goals

## Track 12: Web CTA — Paste URL, Generate Personas, Run Analysis
   Owner: src/lib/generate-personas.ts (CREATE), src/app/api/evaluate/route.ts (CREATE),
          src/components/report-card.tsx (CREATE), src/app/page.tsx (MODIFY)
   Dep: pnpm add @ai-sdk/react (for streaming hooks)
   Purpose: Landing page CTA where anyone can paste a URL and get a full evaluation
   [ ] Create generate-personas.ts — Gemini generates 5 personas specific to the page content
       — takes screenshot Buffer + optional URL
       — system prompt: "generate personas relevant to THIS page, not generic ones"
       — returns Persona[] tailored to the page (e.g., cooking site → food personas)
   [ ] Create /api/evaluate route — POST { url } → screenshot → generate personas → evaluate all → JSON
   [ ] Create report-card.tsx component — displays evaluations with persona cards, scores, strengths/weaknesses
   [ ] Add CTA section to page.tsx below existing hero
       — input field: "Paste any URL"
       — submit button: "Deploy Phantom Users"
       — phased loading: screenshotting → generating personas → evaluating → done
       — results stream in per persona as they complete
   [ ] (Stretch) Stream results via SSE instead of waiting for all 5
   VERIFY: paste URL on localhost:3000 → personas generated → evaluations shown
   VERIFY: generated personas are specific to the page (not generic)

=====================================
NOTES / BLOCKERS
=====================================
- .env.local contains real API keys — this is a PUBLIC repo. Do NOT commit .env.local.
  .gitignore covers .env*.local but double-check before every commit.
- Need to install before Batch 1: @ai-sdk/google, zod, puppeteer-core, @sparticuz/chromium
- Screenshot approach: LOCAL Puppeteer (puppeteer-core + @sparticuz/chromium). No external API.
  - Dev: uses local Chrome binary
  - Prod (Vercel): uses @sparticuz/chromium (~50MB, fits in serverless)
  - Fallback: if Puppeteer fails on Vercel, can swap to free screenshot API later
- Chat SDK bot.tsx currently has ~840 lines of example handlers. GhostShip handlers
  need to be added. Decide: replace all example handlers, or add alongside them.
- The Vercel AI SDK agent import in bot.tsx uses "anthropic/claude-4.5-sonnet" for AI mode.
  GhostShip evaluation will use Gemini via @ai-sdk/google — these are separate concerns.
